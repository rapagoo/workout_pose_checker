# 개발 트러블슈팅 기록

Workout Pose Checker를 개발하면서 확인한 문제와 해결 방법을 기록합니다.
README에는 대표 사례만 요약하고, 이 문서에는 문제의 원인과 현재 구현의 한계,
후속 개선 방향을 함께 정리합니다.

## 1. 여러 사람이 화면에 잡히면 분석 대상이 바뀌는 문제

### 증상

- 웹캠에 두 명 이상이 보이면 자세 인식이 불안정해졌습니다.
- 프레임마다 다른 사람의 관절을 분석하여 운동 상태와 반복 횟수가 갑자기
  변경될 수 있었습니다.

### 원인

YOLO Pose는 한 프레임에서 여러 사람의 바운딩 박스와 키포인트를 반환합니다.
분석 대상을 명시적으로 선택하지 않으면 UI와 Analyzer가 어느 사람의 자세를
처리해야 하는지 일관되게 결정하기 어렵습니다.

### 적용한 해결 방법

`PoseService.analyze_frame()`에서 감지된 각 사람의 바운딩 박스 넓이를 계산하고,
화면에서 가장 크게 잡힌 한 명의 키포인트만 Analyzer에 전달하도록 변경했습니다.

관련 파일:

- `src/workout_pose_checker/pose_service.py`

### 남은 개선 과제

현재 방식은 매 프레임 가장 큰 사람을 선택하므로 두 사람의 화면 점유율이
비슷하거나 서로 앞뒤로 움직이면 대상이 바뀔 수 있습니다. 향후에는 다음 방법을
검토할 수 있습니다.

- 이전 프레임의 바운딩 박스와 가장 가까운 사람을 계속 선택
- YOLO Tracking을 이용한 사람 ID 유지
- 분석 대상이 바뀌기 전 일정 프레임의 유예 구간 적용
- UI에서 사용자가 분석 대상을 직접 선택

## 2. 몸의 일부가 화면 밖으로 나가면 자세를 판정하지 못하는 문제

### 증상

- 전신이나 운동 판정에 필요한 관절이 보이지 않으면 인식이 풀렸습니다.
- 팔굽혀펴기에서 손목, 엉덩이 또는 발목이 가려지면 상태 판정이 자주
  초기화되었습니다.

### 원인

운동 판정은 여러 관절의 좌표와 각도를 함께 사용합니다. 필요한 키포인트의
신뢰도가 기준보다 낮거나 프레임 밖에 있으면 관절 각도를 안정적으로 계산할 수
없습니다.

### 적용한 해결 방법

Analyzer에서 운동별 필수 관절의 신뢰도를 확인하고, 판정에 필요한 관절이
충분히 감지되지 않은 프레임은 성공 또는 실패 횟수에 반영하지 않도록 했습니다.
좌우 측면 중 더 잘 보이는 쪽의 관절을 선택해 분석하도록 구성했습니다.

관련 파일:

- `src/workout_pose_checker/analyzers/squat.py`
- `src/workout_pose_checker/analyzers/pushup.py`
- `src/workout_pose_checker/pose_utils.py`

### 남은 개선 과제

- 운동 시작 전에 권장 카메라 위치를 안내하는 가이드 화면 제공
- 운동별 필수 관절이 가려졌을 때 구체적인 안내 메시지 표시
- 화면 위에 전신이 들어와야 하는 영역 또는 실루엣 가이드 표시
- 실제 촬영 환경을 반영한 신뢰도 임계값 재조정

## 3. UI와 포즈 분석 로직이 강하게 결합된 문제

### 증상

- UI를 실행하지 않고 자세 판정 로직만 테스트하기 어려웠습니다.
- 운동을 추가하거나 분석 결과 형식을 바꾸면 UI 코드도 함께 수정해야 했습니다.
- 모델 추론이 필요한 탓에 페이지 이동과 결과 화면을 빠르게 테스트하기
  어려웠습니다.

### 원인

웹캠 입력, YOLO 추론, 운동 판정, UI 상태 변경이 한 흐름 안에서 함께 처리되고
있었습니다. 각 코드의 책임과 데이터 전달 형식이 명확히 분리되지 않은 것이
원인이었습니다.

### 적용한 해결 방법

- `PoseService`를 UI와 모델 사이의 단일 연동 지점으로 정의했습니다.
- 스쿼트와 팔굽혀펴기 판정 로직을 각각의 Analyzer로 분리했습니다.
- UI가 사용하는 분석 결과 형식을 공통 딕셔너리 형태로 통일했습니다.
- 실제 모델 없이 UI 흐름을 검증할 수 있도록 `MockPoseService`를 추가했습니다.

관련 파일:

- `src/workout_pose_checker/pose_service.py`
- `src/workout_pose_checker/mock_pose_service.py`
- `src/workout_pose_checker/analyzers/`
- `docs/model_ui_contract.md`

### 확인 방법

`src/workout_pose_checker/main_window.py`에서 다음 값을 변경하면 Mock 서비스를
사용할 수 있습니다.

```python
USE_MOCK_POSE_SERVICE = True
```

실제 모델 연동으로 돌아가려면 값을 `False`로 변경합니다.

## 4. 정면과 측면 구도가 반복해서 전환되는 문제

### 증상

- 사용자가 카메라를 향해 비스듬히 서 있으면 정면과 측면 판정이 반복해서
  바뀌었습니다.
- 구도가 바뀔 때마다 사용하는 관절과 운동 상태가 달라져 횟수 판정이
  불안정해졌습니다.

### 원인

하나의 임계값으로 정면과 측면을 구분하면 측정값이 경계 부근에서 조금만
변해도 매 프레임 판정 결과가 뒤집힐 수 있습니다.

### 적용한 해결 방법

어깨 너비와 몸통 길이의 비율을 이용하여 정면과 측면을 구분했습니다. 현재
구도에 따라 정면 진입 임계값과 이탈 임계값을 다르게 적용하는 히스테리시스를
추가하여 경계 부근의 잦은 전환을 줄였습니다.

관련 파일:

- `src/workout_pose_checker/analyzers/squat.py`
- `src/workout_pose_checker/analyzers/pushup.py`

### 남은 개선 과제

- 체형과 카메라 거리가 다른 사용자 데이터로 임계값 검증
- 구도 변경을 확정하기 전에 여러 프레임의 평균값 사용
- 현재 인식된 구도와 권장 자세를 UI에 더 명확히 표시

## 5. 운동 종료 후 카메라가 계속 점유되는 문제

### 증상

- 운동 화면을 나간 뒤 카메라를 다시 열지 못할 수 있었습니다.
- 카메라 표시가 중단되어도 프레임 타이머가 계속 동작할 수 있었습니다.

### 원인

운동 포기, 목표 달성, 창 닫기처럼 종료 경로가 여러 개이므로 각 경로에서
카메라와 타이머를 따로 정리하면 일부 경로에서 자원 해제가 빠질 수 있습니다.

### 적용한 해결 방법

`ExercisePage.stop_camera()`에서 카메라 타이머 중지와 `VideoCapture.release()`를
함께 처리하도록 정리했습니다. 운동 포기와 정상 완료 시 이 메서드를 공통으로
호출하도록 구성했습니다.

관련 파일:

- `src/workout_pose_checker/pages/exercise_page.py`

### 확인 방법

1. 운동을 시작하여 웹캠 영상이 표시되는지 확인합니다.
2. `포기하기` 버튼으로 운동 선택 화면으로 돌아갑니다.
3. 운동을 다시 시작했을 때 카메라가 정상적으로 열리는지 확인합니다.
4. 목표를 달성하여 결과 화면으로 이동한 뒤에도 같은 과정을 확인합니다.

## 6. 같은 자세에서 운동 횟수가 중복 집계되는 문제

### 증상

완료 자세를 몇 프레임 동안 유지하면 같은 동작이 여러 번 성공으로 집계될 수
있었습니다.

### 원인

각 프레임의 관절 각도만 독립적으로 검사하면 완료 조건을 만족하는 모든 프레임을
새로운 반복으로 잘못 처리할 수 있습니다.

### 적용한 해결 방법

Analyzer 내부에 운동 진행 상태를 저장하고, 준비 자세를 거쳐 완료 자세로
전환되었을 때만 반복 횟수를 증가시키도록 변경했습니다. 사람을 찾지 못한
프레임은 `PERSON_NOT_FOUND` 상태로 반환하고, 운동을 새로 시작할 때는
`PoseService.reset()`을 통해 해당 Analyzer의 횟수와 상태를 초기화합니다.

관련 파일:

- `src/workout_pose_checker/analyzers/squat.py`
- `src/workout_pose_checker/analyzers/pushup.py`
- `src/workout_pose_checker/pose_service.py`

### 확인 방법

분석기 단위 테스트를 실행합니다.

```powershell
python -m pytest tests/test_squat_analyzer.py tests/test_pushup_analyzer.py -q
```

## 전체 회귀 테스트

트러블슈팅 과정에서 로직을 변경한 뒤에는 프로젝트 루트에서 전체 테스트를
실행합니다.

```powershell
python -m pytest -q
```

자동 테스트 외에도 실제 웹캠 환경에서 다음 항목을 확인해야 합니다.

- 운동 선택부터 결과 화면까지 정상적으로 이동하는지
- 스쿼트와 팔굽혀펴기가 올바른 Analyzer로 전달되는지
- 정면과 측면 구도가 불필요하게 반복 전환되지 않는지
- 운동 포기와 정상 완료 후 카메라가 해제되는지
- 화면에 다른 사람이 들어왔을 때 분석 대상이 어떻게 선택되는지
